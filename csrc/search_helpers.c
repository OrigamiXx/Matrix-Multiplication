#include <queue>
#include "searcher2.h"
#include "puzzle.h"
#include "ExtensionGraph.hpp"
#include "canonization.h"

bool null_reduce(unsigned long a, unsigned long b, void * c) {
  // we're going to get the label_u (which is the row) and we tack that on the puzzle

  // do a struct to contain both the queue and the puzzle   
  return false;

}

std::priority_queue<heuristic_result> individual_heuristics(puzzle * p, ExtensionGraph * eg, heuristic_t ht) {
  
  std::priority_queue<heuristic_result> hrq;
  ExtensionGraph neg(*eg);
     
  

  return hrq; 

}
